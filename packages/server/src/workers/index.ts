// Sync worker removed — Gmail mirror infrastructure deleted.
// Calendar sync is handled directly by calendar-sync.service.ts.

export async function startSyncWorker(): Promise<void> {
  // no-op: Gmail sync worker removed
}

export async function stopSyncWorker(): Promise<void> {
  // no-op: Gmail sync worker removed
}
